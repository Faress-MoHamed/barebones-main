import React, { useEffect, useState } from "react";
import { View } from "react-native";

export default function IsLoginProvider() {
	const [isLogged, setIsLogged] = useState<boolean>(false);

  useEffect(() => {
    
  }, []);

	return <View></View>;
}
